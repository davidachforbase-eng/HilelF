# Public Assets

This folder is for static assets like images, fonts, and icons.
Files placed here can be referenced in your code with a root-relative path.

Example:
`public/hillel.jpg` -> `src="/hillel.jpg"`
